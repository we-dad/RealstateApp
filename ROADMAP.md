# ROADMAP

خارطة الطريق للمشروع. الترتيب في الجدول حسب الأهمية، وعمود "التنفيذ" هو الترتيب المنطقي للعمل.

- الأحجام تقديرية: **صغير** أقل من يوم، **متوسط** من 1 إلى 3 أيام، **كبير** أكثر من ذلك.
- كل مهمة يمس كود المزامنة (1 و2 و3 و4) يحتاج خطة وموافقة المطوّر قبل التنفيذ.
- مهمة 9 يحتاج إذن المطوّر لتغيير الحزم، ويأتي بعد مهمة 12 (اختبارات المزامنة) لأن تحديث الحزم قد يكسر البناء.
- مهمة 12: الاختبارات تعمل على SQLite مؤقت في الذاكرة مع سحابة وهمية، ولا تلمس القاعدة الحقيقية ولا Supabase.
- مهمة 13 ينفذه المطوّر يدويًا (Claude لا يتصل بـ Supabase). اختبار RLS العملي ضمن مهمة 8 لأنه يحتاج مشروع التطوير.
- أرقام عمود "#" معرّفات ثابتة ولا تتغير عند إعادة الترتيب.

## نظرة عامة

| الأهمية | # | المهمة | الحجم | التنفيذ | ملاحظة |
|---|---|---|---|---|---|
| 1 | 13 | مراجعة سياسات RLS في Supabase (نظريًا من اللوحة) | صغير | 2 | يدوي من المطوّر. الاختبار العملي ضمن مهمة 8 |
| 2 | 1 | إصلاح سباق الرفع/السحب | متوسط | 3 | يسبب تكرار صفوف. الحل النموذجي موجود في شاشة الوحدات |
| 3 | 2 | منع تكرار الصفوف في UpsertFromCloud | متوسط | 4 | يكمل مهمة 1 ويحمي من صفوف بلا `CloudId` |
| 4 | 5 | رسالة واضحة لدور tester | صغير | 1 | سريع، بدون مساس بمنطق المزامنة |
| 5 | 4 | توحيد منطق المزامنة في مكان واحد | كبير | 5 | يحوّل 12 نسخة إلى مكان واحد |
| 6 | 12 | اختبارات آلية لمنطق المزامنة | كبير | 6 | تُكتب بعد توحيد المنطق (مهمة 4) |
| 7 | 9 | تحديث الحزم الأربع ذات الثغرات الأمنية | متوسط | 7 | يحتاج إذن المطوّر. بعد مهمة 12 لأنه قد يكسر البناء |
| 8 | 3 | زر "استرجاع شامل" بمؤشر تقدم | كبير | 10 | يحتاج 1 و2 و4. يظهر لـ admin و editor و viewer فقط |
| 9 | 6 | نقل إعدادات Supabase إلى ملف خارجي | متوسط | 8 | شرط لمهام 7 و8 |
| 10 | 8 | مشروع Supabase منفصل للتطوير | صغير | 9 | يحتاج 6. الإنشاء يدوي من المطوّر. يشمل اختبار RLS العملي |
| 11 | 7 | وضع تجريبي ببيانات وهمية وبدون سحابة | كبير | 11 | يحتاج 6 |
| 12 | 10 | توقيع نسخة macOS | متوسط | 12 | يحتاج حساب Apple Developer |
| 13 | 11 | تطوير مستودع العرض (صور ووصف وفيديو) | كبير | 13 | ميزة جديدة، آخر أولوية |
| 0 | 14 | سلامة سندات القبض ورصيد العقد بين أكثر من جهاز | متوسط | 0 | عاجل: يمس المال. المرحلة 1 (اشتقاق الرصيد) منجزة ومختبرة، والمرحلة 2 (رقم سند لا يتعارض) لم تبدأ |
| 0 | 15 | إصلاح حسبة الدفعة المقدمة في صفحة تعديل العقد | صغير | 0 | منجز. يبقى فحص العقود التي انحفظت غلط قبل الإصلاح (بمراجعة المطوّر مع العميل) |
| 0 | 16 | قوائم بحث ذكية بالاسم والهوية ورقم العقد | متوسط | 0 | منجز ومختبر |
| 0 | 17 | تحديث البيانات تلقائيًا | متوسط | 0 | منجزة ومختبرة |
| 0 | 23 | ملاحظات المزامنة المؤجلة | متوسط | 6 | من مراجعة المزامنة الدورية. تحتاج قرارات |
| 0 | 22 | نسخ النصوص المعروضة | صغير | 1 | تحديد النصوص للنسخ |
| 0 | 19 | بحث داخل الجداول | متوسط | 2 | خانة بحث فوق كل جدول |
| 0 | 21 | رقم السند التسلسلي في السند المطبوع (تلقائي أو يدوي) | متوسط | 3 | يحتاج عمودًا جديدًا في Supabase | 
| 0 | 20 | إجمالي المبلغ والمتبقي في عقد الأقساط | صغير | 4 | بانتظار تأكيد المثال | 
| 0 | 24 | خط IBM Plex Sans Arabic يعطّل التطبيق | صغير | 0 | عاجل: مؤجَّل حتى إيجاد بديل. رمز الخروج 134 عند فتح التطبيق | 
| 0 | 25 | وضع داكن (Dark Mode) للهوية الجديدة | متوسط | 0 | التطبيق مثبَّت على الوضع الفاتح حاليًا؛ الوضع الداكن مهمة لاحقة | 
| 0 | 26 | إعادة تصميم شاشات الأقساط على هوية العميل | كبير | 0 | تجربة أولى منجزة على شاشة سند القبض؛ التعميم على باقي الشاشات ينتظر اختبار المطوّر | 
| 0 | 18 | بيانات العقد ثابتة بعد الإنشاء (الأقساط فقط) | كبير | 5 | يحتاج أعمدة جديدة في Supabase | 

---

## 1. إصلاح سباق الرفع/السحب (متوسط)
الرفع والسحب يعملان معًا بدون انتظار في أغلب الشاشات، فقد تتكرر الصفوف. شاشة الوحدات وحدها تنتظر الرفع أولًا.

- [x] تحديد كل الشاشات التي تستدعي `PushAllDirtyAsync` و`Sync...FromCloudAsync` بدون انتظار
- [x] كتابة خطة والحصول على موافقة المطوّر
- [x] انتظار اكتمال الرفع قبل السحب في كل الشاشات (زر "تحديث" وفتح الشاشة): 11 شاشة قائمة (5 عقارات و6 أقساط) على فرع `fix-push-pull-race`، وبوابة الرفع صارت تنتظر بدل أن تُسقط الرفع الثاني، وأُضيفت بوابة لرفع الأقساط
- [x] مراجعة التعليق في `UnitsViewRealEstate` (السطران 38 و39) بعد الإصلاح: ما زال صحيحًا، وهو نفسه المنسوخ في بقية الشاشات
- [x] نافذتا العقود والمصاريف في الأقساط (`ContractWindowViewInstallment` و`ExpensesWindowViewInstallment`): كان لكل منهما نسخة رفع خاصة تتجاوز `InstallmentSyncService` وبوابته (المصاريف تستخدم خدمة العقارات، والعقود بلا حقول التوقيع). فحص المطوّر Supabase ولم يجد بيانات متضررة. استُبدلت النسختان باستدعاء `PushAllDirtyAsync` (commit `3250d98`)
- [x] اختبار المطوّر للنافذتين: تعديل عقد (التوقيع لا يُمسح)، وإضافة مصروف (يدخل `expenses_installment` وليس `expenses_real_estate`). أفاد المطوّر أن كل شيء سليم

## 2. منع تكرار الصفوف في UpsertFromCloud (متوسط)
المطابقة الآن بـ `CloudId` فقط، فصف محلي جديد لم يُرفع ثم نزل من السحابة يظهر مكررًا.

- [x] كتابة خطة والحصول على موافقة المطوّر
- [x] تحديد مفتاح مطابقة احتياطي لكل جدول عندما يكون `CloudId` فارغًا: الهوية والاسم للملاك والمستأجرين والعملاء، والرقم مع التاريخ والمبلغ والأب للعقود والإيصالات والمصاريف، والمالك مع الاسم والمدينة والحي والنوع والرقم للوحدات، ورقم الهيكل أو كل حقول المنتج للمنتجات
- [x] تطبيق المنطق في الخدمات الـ12 مع الحفاظ على حماية `IsDirty = 1`: التبنّي يغيّر `CloudId` و`SyncAction` فقط (من `insert` إلى `update`)، وخطأ `UNIQUE` في الجداول الستة يتخطى الصف ولا يوقف السحب. على فرع `fix-upsert-duplicates` (commits `37c5b65` و`f5dbfd8` و`96e74d8` وآخرها للوحدات والمنتجات)
- [x] اختبار المطوّر (الاستخدام العادي في كل الشاشات، وإضافة عقد وإيصال ومصروف، وجوالان بلونين مختلفين، ومبنى بوحداته): سليم، ثم دُمج الفرع في `claude-setup`
- [ ] (مهمة لاحقة) رسالة عربية للمستخدم عند تخطي صف بسبب تعارض رقم، لأن التخطي الآن يظهر في Console فقط، وإيصالات ومصاريف العقد المتخطى تُتخطى معه

## 3. زر "استرجاع شامل" (كبير)
يسحب كل الجداول بالترتيب الصحيح (الأب قبل الابن) مع مؤشر تقدم.

- [ ] كتابة خطة والحصول على موافقة المطوّر
- [ ] تحديد ترتيب السحب (المُلّاك والوحدات والمستأجرون قبل العقود، ثم الإيصالات والمصاريف، ومثله في التقسيط)
- [ ] إظهار الزر لأدوار admin و editor و viewer فقط (`AppSession.CanReadOnline`)
- [ ] إخفاء الزر في الوضع التجريبي (مهمة 7)
- [ ] مؤشر تقدم ورسالة نتيجة (عدد الصفوف وما تم تخطيه)
- [ ] عدم الكتابة فوق `IsDirty = 1` وعدم حذف أي صف محلي

## 4. توحيد منطق المزامنة (كبير)
منطق السحب مكرر في 12 شاشة.

- [ ] كتابة خطة والحصول على موافقة المطوّر
- [ ] استخراج المنطق المشترك إلى خدمة واحدة
- [ ] نقل الشاشات واحدة واحدة، مع بناء `dotnet build` بعد كل شاشة
- [ ] حذف الكود المكرر بعد اكتمال النقل

## 5. رسالة واضحة لمستخدم دور tester (صغير)
الدور الافتراضي `tester` لا يزامن شيئًا، والمستخدم لا يعرف السبب.

- [x] تحديد مكان عرض الرسالة (بعد الدخول أو داخل الشاشات)
- [x] كتابة رسالة عربية توضح أن الحساب بلا صلاحية مزامنة
- [x] عرضها عندما يكون الدور `tester`

## 6. نقل إعدادات Supabase إلى ملف خارجي (متوسط)
- [ ] تحديد الإعدادات الموجودة حاليًا في الكود
- [ ] اختيار مكان الملف (خارج المستودع) وصيغته
- [ ] القراءة من الملف مع رسالة واضحة إذا كان ناقصًا
- [ ] إضافة الملف إلى `.gitignore` وتوثيق الخطوات في README (بدون أي مفاتيح)

## 7. وضع تجريبي ببيانات وهمية وبدون سحابة (كبير)
شاشة اختيار عند الفتح: دخول عادي أو وضع تجريبي.

- [ ] تصميم شاشة الاختيار
- [ ] قاعدة SQLite منفصلة للوضع التجريبي (لا تلمس القاعدة الحقيقية)
- [ ] بيانات وهمية للوحدتين
- [ ] تعطيل كل استدعاءات السحابة في الوضع التجريبي
- [ ] إخفاء زر "استرجاع شامل" في الوضع التجريبي
- [ ] إشارة ظاهرة أن التطبيق في الوضع التجريبي

## 8. مشروع Supabase منفصل للتطوير (صغير)
ينفذه المطوّر يدويًا.

- [ ] إنشاء مشروع Supabase جديد للتطوير
- [ ] نسخ المخطط (schema) إليه من المشروع الحقيقي
- [ ] تفعيل RLS وتعطيل التسجيل العام فيه كما في الإنتاج
- [ ] ربطه عبر ملف الإعدادات الخارجي (مهمة 6)
- [ ] اختبار RLS عمليًا (من مهمة 13): تسجيل الدخول بحساب `tester` على مشروع التطوير ومحاولة القراءة والكتابة، ولا يُختبر ذلك على الإنتاج

## 9. تحديث الحزم الأربع ذات الثغرات الأمنية (متوسط)
الحزم: `Microsoft.IdentityModel.JsonWebTokens`، `System.IdentityModel.Tokens.Jwt`، `SQLitePCLRaw.lib.e_sqlite3`، `Tmds.DBus.Protocol`.

- [ ] الحصول على إذن المطوّر لتغيير الحزم
- [ ] تحديد الإصدارات الآمنة لكل حزمة
- [ ] تحديث حزمة واحدة في كل مرة مع `dotnet build`
- [ ] تشغيل التطبيق للتأكد من الدخول والمزامنة (ينفذه المطوّر)
- [ ] إزالة هذه الحزم من قسم Known issues في `CLAUDE.md`

## 10. توقيع نسخة macOS (متوسط)
- [ ] التأكد من وجود حساب Apple Developer
- [ ] إنشاء شهادة التوقيع
- [ ] توقيع النسخة وإرسالها للتوثيق (notarization)
- [ ] توثيق الخطوات

## 11. تطوير مستودع العرض: صور ووصف وفيديو (كبير)
- [ ] تحديد الشاشات والجداول المعنية
- [ ] كتابة خطة تشمل التخزين (محلي أم سحابي) وحجم الملفات
- [ ] تصميم الشاشة
- [ ] التنفيذ

## 12. اختبارات آلية لمنطق المزامنة (كبير)
تعمل على SQLite مؤقت في الذاكرة مع سحابة وهمية، بدون Supabase وبدون القاعدة الحقيقية.

- [ ] إضافة مشروع اختبارات (يحتاج إذن المطوّر لأنه يضيف حزم NuGet)
- [ ] واجهة سحابة وهمية (fake) للاختبارات
- [ ] اختبار: السحب لا يكتب فوق `IsDirty = 1`
- [ ] اختبار: السحب لا يحذف صفوفًا محلية
- [ ] اختبار: تخطي الصفوف عند غياب الأب
- [ ] اختبار: منع التكرار (مهمة 2)
- [ ] اختبار: الرفع قبل السحب (مهمة 1)

## 13. مراجعة سياسات RLS في Supabase (صغير، يدوي)
الهدف: التأكد أن المستخدم المسجّل لا يقدر يقرأ بيانات غير مسموحة له. ينفذه المطوّر يدويًا، وClaude لا يتصل بـ Supabase. الاختبار العملي (بحساب tester) في مهمة 8 لأنه يحتاج مشروع التطوير.

الخطوات:
- [ ] فتح لوحة Supabase، ثم Authentication ثم Policies
- [ ] التأكد أن RLS مفعّل على كل الجداول (12 جدولًا للأعمال + جدول الأدوار)
- [ ] لكل جدول: قراءة السياسات والتأكد أن SELECT/INSERT/UPDATE/DELETE مربوطة بالدور (admin و editor و viewer) وليست `true` لأي مستخدم مسجّل
- [ ] التأكد أن دور `viewer` لا يقدر يكتب أو يحذف
- [ ] التأكد أن دور `tester` أو أي مستخدم بلا دور لا يقرأ أي جدول
- [ ] التأكد أن المستخدم لا يقدر يعدّل جدول الأدوار الخاص به (منع رفع صلاحيته لنفسه)
- [ ] تسجيل النتائج والملاحظات في `PROGRESS.md`

---

## 14. سلامة سندات القبض ورصيد العقد بين أكثر من جهاز (متوسط، عاجل)
اكتشفه فحص `sync-auditor`: رصيد العقد (`CurrentTotalAmount`) رقم مخزّن يتعدّل محليًا بدون أن يُعلَّم العقد للرفع، فيفوز آخر جهاز يكتب ولا يُدمج شيء. ورقم السند يُولَّد محليًا فيتعارض بين جهازين. وتعديل عقد عليه سندات يعيد رصيده للأصل.

**تحذير حتى يكتمل الإصلاح ويُدمج: لا تعدّل عقدًا عليه سندات، ولا تضف سندات لنفس العقد من جهازين بدون مزامنة بينهما.**

- [x] اشتقاق الرصيد من السندات: `الرصيد = المبلغ الأصلي − مجموع السندات غير المحذوفة` (لا ينزل عن صفر)، ويُعاد بعد إضافة سند أو تعديله أو حذفه، وبعد تعديل العقد، وبعد سحب السندات. وعند سحب العقد لا يُعاد الحساب إلا إذا كانت السندات موجودة محليًا (وإلا يبقى رصيد السحابة حتى تصل السندات). على فرع `fix-receipt-balance`
- [x] اختبار المطوّر للرصيد (إضافة سندين، وتعديل عقد عليه سندات، وحذف سند)، ثم دمج الفرع في `claude-setup`. أفاد المطوّر أن كل شيء سليم
- [x] رقم السند والمصروف بدون تعارض بين المستخدمين: رمز من 3 خانات مشتق من رقم حساب المستخدم (`Ir-1001-K7Q`)، مع دعم الأرقام القديمة، ولا ملف ولا إعداد. قرر المطوّر أن لكل شخص حسابه وجهازه، فاكتفينا برمز المستخدم بدل رمز الجهاز. على فرع `fix-document-number-collision`
- [x] رقم **العقد** صار بالرمز أيضًا (`Ic-1051-K7Q` و`Rc-1051-K7Q`)، والبحث في شاشات السندات يقبل الرقم القصير (`1051` أو `Ic-1051`) والكامل والقديم، وإذا تطابق أكثر من عقد يعرض الأرقام الكاملة ويطلب كتابة الكامل
- [ ] نافذة إيصالات العقارات (`ReceiptsWindowViewRealEstate`) لا تضيف البادئة `Rc-` عند البحث، فكتابة `1001` وحدها لا تنجح هناك (قبل التغيير وبعده)
- [ ] نفس الحساب على جهازين بدون اتصال قد يكرر الرقم (الرمز واحد للحساب). إن احتاج المطوّر ذلك يُضاف رقم جهاز
- [ ] السند المتخطى بسبب تعارض رقم لا يظهر للمستخدم (رسالة عربية)
- [ ] حذف سند على جهاز لا ينتقل للجهاز الآخر (السحب لا يحذف صفوفًا محلية): مهمة مستقلة تغيّر قاعدة "السحب لا يحذف"، ويرتبط بمهمة 3
- [ ] فحص قيود Supabase على رقم السند (يدويًا من المطوّر)
- [ ] فحص سندات العقارات: هل فيها نفس التصميم؟
- [ ] لقطة الرصيد داخل السند (`CurrentTotalAmount` في `ReceiptsInstallment`) ما زالت تراكمية وقد تتضارب عبر الأجهزة، والـ PDF يطبعها في سطر 559
- [ ] عقود تضررت أرصدتها سابقًا: لا تُصلح تلقائيًا إلا عند أول عملية على سنداتها، وقرار إعادة حساب الكل يعود للمطوّر

---

## 15. حسبة الدفعة المقدمة في صفحة تعديل العقد (صغير)
كانت صفحة التعديل تعامل سعر المنتج الكامل كأنه سعر بعد الدفعة المقدمة، فتُحسب الدفعة مرتين، ويُحفظ الإجمالي بدون خصمها عند تغيير أي حقل.

- [x] إصلاح النافذة: السعر الكامل ثم خصم الدفعة، وعند تغيير المنتج، وعند إلغاء تحديد الدفعة أو مسح خانتها (`ContractWindowViewInstallment`)
- [x] تقرير "فحص الحسابات" للقراءة فقط في شاشة عقود الأقساط: يقارن الإجمالي المحفوظ بالمعادلة، ويعرض السعر الأصلي المقابل للمحفوظ (لو انخصمت الدفعة، ولو لم تنخصم) دون الاعتماد على سعر المنتج الحالي
- [x] راجع المطوّر العقود مع العميل: أغلب الفروق سببها أن العميل يغيّر أسعار المنتجات بعد بيعها، وليست خطأ في البرنامج. وهذا سبب مهمة 18 (فصل العقد عن المنتج بعد إنشائه)
- [ ] نفس خطأ إلغاء تحديد الدفعة في صفحة **إنشاء** العقد (`ContractViewInstallment`): لم يُصلح لأن المطوّر أفاد أن الإنشاء يعمل، وبانتظار قراره
- [ ] الوضع اليدوي: إلغاء الدفعة أو إعادتها يستبدل الإجمالي المكتوب يدويًا بالمحسوب. بانتظار قرار المطوّر

---

## 16. قوائم بحث ذكية (متوسط)
خانات البحث في التطبيق صارت قوائم اقتراحات تعرض الاسم جنب الرقم، والاختيار منها يحدد الشخص أو العقد بدون الحاجة لزر "بحث".

- [x] العقد في شاشات السندات ونوافذ تعديلها (الأقساط والعقارات): `رقم العقد — العميل أو المستأجر — المنتج أو الوحدة`، مع "(منتهي)" للعقود المنتهية
- [x] العميل بالهوية في شاشة العقد ونافذتها (الأقساط)، والمستأجر بالهوية في العقارات: `الهوية — الاسم`
- [x] الاختيار بالرقم الداخلي (Id)، وتغيير النص بعد الاختيار ينسى الاختيار حتى لا يُحفظ سجل على شخص أو عقد غير ظاهر
- [x] رقم العقد صار نصًا للقراءة فقط (لا يُعدَّل أبدًا، حتى في الوضع اليدوي)، وأرقام العقود والسندات والمصاريف صارت بها رمز المستخدم (`Ic-1051-K7Q`) مع بحث يقبل الرقم القصير والكامل والقديم
- [ ] لوحتا التحكم لا تستخدم هذه القوائم

## 17. تحديث البيانات تلقائيًا (متوسط)
- [x] المرحلة 1: كل شاشة قائمة (11 شاشة) تعيد تحميل جدولها من قاعدة البيانات المحلية بعد أي إضافة أو تعديل أو حذف، حتى من نافذة التفاصيل، بدون ضغط "تحديث" ولا مسح خانات الإدخال (`DataChangeNotifier` و`ScreenAutoRefresh`)
- [x] المرحلة 2: سحب دوري كل دقيقتين على الشاشات الـ12 (ارفع ثم اسحب، أثناء نشاط التطبيق فقط)، بقفل يمنع تداخل مزامنتين (طلب المستخدم ينتظر دوره، والمؤقت يتخطى)، ويحفظ الصف المحدد. القوائم المنسدلة للمنتج والوحدة تحتفظ باختيارها ولا يتغير الحساب. اختبر المطوّر شاشة الملاك (الأقساط) وأفاد أنها سليمة، والتعميم بانتظار اختباره
- [x] فحص `CanWriteOnline` في رفع الأقساط (مثل العقارات)، وفحص `CanReadOnline` الناقص في سحب الوحدات
- [x] مؤشر المزامنة في الشاشتين الرئيسيتين (`SyncStatusService`): آخر مزامنة ناجحة، أو فشل الرفع أو السحب أو انقطاع الاتصال
- [x] لوحتا التحكم (الأقساط والعقارات) تتحدثان تلقائيًا بعد أي تغيير
- [x] اختبر المطوّر كل ما سبق (الملاك ثم التعميم على الشاشات كلها) وأفاد أنه سليم

---

## 22. نسخ النصوص المعروضة (صغير)
النصوص المعروضة (تفاصيل العقد، معلومات العميل والمستأجر...) لا تُنسخ الآن.

- [ ] جعل النصوص المعروضة قابلة للتحديد والنسخ (`SelectableTextBlock`) في كل الشاشات والنوافذ
- [ ] مراجعة كل الشاشات للتأكد أن كل ما يحتاجه المستخدم قابل للنسخ

## 19. بحث داخل الجداول (متوسط)
يبحث عن عميل معين أو عقد أو أي شيء داخل الجدول.

- [x] خانة بحث فوق الجدول تفلتر الصفوف بأي نص (رقم أو اسم...)
- [x] على شاشات القوائم الـ12 (الأداة المشتركة `GridSearch`)
- [x] الفلتر يبقى بعد إعادة تحميل الجدول (التحديث التلقائي) ويحفظ الصف المحدد
- [ ] اختبار المطوّر

## 21. رقم السند التسلسلي في السند المطبوع، تلقائي أو يدوي (متوسط)
السند المطبوع يحمل أسفله "السند رقم N". حاليًا N يُحسب عند الطباعة من ترتيب السند داخل العقد (وقد يتغير لو حُذف سند). المطلوب: رقم يُحفظ مع السند، تلقائي أو يكتبه المستخدم (لأن عند العميل سندات قديمة لم تُدخل)، وتُعرض السندات مرتبة به.

- [ ] عمود جديد لرقم السند التسلسلي في جدول سندات الأقساط (SQLite وSupabase، إضافة فقط، ويشغّل المطوّر ملف SQL بنفسه قبل تحديث التطبيق)
- [ ] الرقم التلقائي = آخر رقم في نفس العقد + 1 (مع احتساب السندات القديمة)، أو يكتبه المستخدم في شاشة السند
- [ ] منع تكرار الرقم داخل نفس العقد، برسالة عربية
- [ ] الرقم يُحفظ مع السند ويُطبع في "السند رقم N"، ولا يتغير عند حذف سند آخر
- [ ] عرض السندات مرتبة بهذا الرقم (سجل العميل، وكشف الحساب، ونافذة العقد)
- [ ] السندات القديمة تبقى بترتيبها الحالي إلى أن يُحدَّد لها رقم
- [ ] مزامنة العمود الجديد مع السحابة (تمس المزامنة، تحتاج خطة وموافقة)

## 20. إجمالي المبلغ والمتبقي في عقد الأقساط (صغير)
في شاشة العقد الاسم "اجمالي قيمة المنتج" غير صحيح. المطلوب: "إجمالي المبلغ" هو كل ما دفعه العميل وسيدفعه (الدفعة المقدمة + المتبقي)، و"المتبقي" هو المبلغ بعد الدفعة المقدمة وهو المحفوظ.

- [x] تأكيد المطوّر بمثال: سعر 10000، دفعة 2000، رسوم 100، فائدة 10% لـ12 شهر: المتبقي 8900، وإجمالي المبلغ 10900
- [x] إعادة تصميم حسب طلب المطوّر: خانة "إجمالي المبلغ" فوق (قابلة للتعديل في الوضع اليدوي)، ونص "المتبقي بعد الدفعة المقدمة" يظهر تحتها فقط عند وجود دفعة مقدمة، في شاشة العقد ونافذة التعديل ولوحة بيانات العقد
- [x] لا يتغير أي حساب موجود (المحفوظ والقسط والرصيد): الإجمالي يُحسب عند العرض ولا يُخزَّن
- [x] أصلح خلل قديم في شاشة **إنشاء** العقد: إلغاء تحديد "دفعة مقدمة" ما كان يوقف حسبتها فورًا (نفس الإصلاح الذي كان في نافذة التعديل فقط)
- [x] أصلح خللًا نشأ من إعادة التصميم: الوضع اليدوي كان يمحو كتابة المستخدم حرفًا حرفًا لأن الخانة تعرض قيمة محسوبة؛ صار الحساب يتم عند الخروج من الخانة لا مع كل حرف
- [ ] اختبار المطوّر
- [ ] قرار المطوّر: أسماء قديمة ما زالت لهذا المبلغ في شاشات السندات ("الاجمالي" و"القسط الأساسي") وفي الورقة المطبوعة للعقد

## 18. بيانات العقد ثابتة بعد الإنشاء (كبير)
تُنسخ بيانات المنتج داخل العقد لحظة إنشائه، فلا تتأثر بأي تغيير أو حذف للمنتج بعد ذلك.

- [x] قرار المطوّر: وافق على إضافة أعمدة جديدة في SQLite وفي Supabase (إضافة فقط، ويشغّل المطوّر ملف SQL بنفسه قبل تحديث التطبيق)، وعلى الأقساط فقط
- [ ] كتابة خطة والحصول على موافقة المطوّر (تمس المزامنة والمخطط)
- [ ] نسخ اسم المنتج وسعره وتفاصيله داخل العقد عند إنشائه
- [ ] عرض العقد وورقة العقد المطبوعة من النسخة المحفوظة لا من جدول المنتجات
- [ ] قرار المطوّر: العقود القديمة (تُنسخ بياناتها الحالية أم تبقى مرتبطة)
- [ ] مزامنة الأعمدة الجديدة مع السحابة

---

## 23. ملاحظات المزامنة المؤجلة (متوسط)
وجدتها مراجعة المزامنة الدورية (مهمة 17) ولم تُعالج، وبعضها يحتاج قرار المطوّر:

- [ ] الصفوف التي يُتخطى رفعها أو سحبها لغياب الأب تُعدّ نجاحًا في المؤشر: يُفضّل عرض عددها
- [ ] المشاهد (viewer) يستطيع الإضافة محليًا ولا يُرفع شيء منه: قرار (منع الإضافة أو رسالة "للعرض فقط")
- [ ] السحب يجلب الجدول كاملًا في كل دورة (لا تصفية زمنية)
- [ ] الاختيار المتعدد بـ Ctrl في شاشة عقود الأقساط يفقد التحديد الإضافي عند إعادة تحميل الجدول

---

## 24. خط IBM Plex Sans Arabic يعطّل التطبيق (صغير، عاجل) - منجز
دُمج الخط (مهمة تحسين الواجهة) وتم الرجوع عنه فورًا في نفس اليوم: التطبيق ينهار عند الفتح (رمز الخروج 134، SIGABRT) داخل مصفّف نصوص Avalonia نفسه (`Avalonia.Media.TextFormatting.TextFormatterImpl.ShapeTextRuns`)، عند قياس أول نص تلقائيًا عند فتح النافذة الرئيسية، قبل الوصول حتى لشاشة الدخول.

- [x] تشخيص السبب من رسالة الخطأ التي أرسلها المطوّر: مشكلة توافق بين الخط ومحرك تنسيق النص (HarfBuzz) داخل Avalonia، وليست في كود المشروع
- [x] إرجاع الخط إلى Arial فورًا في `Main/App.axaml` لاستعادة عمل التطبيق؛ ملفات الخط بقيت في `Assets/Fonts` والمورد `ArabicFontFamily` باقٍ معرَّفًا وغير مستخدم لأي محاولة لاحقة
- [x] المطوّر يفضّل نفس الخط ولا يريد التبديل
- [x] المحاولة 2: الإشارة لملف .ttf واحد مباشرة بدل مجلد - فشلت بخطأ مختلف ("Could not create glyphTypeface"، استثناء عادي وليس انهيارًا)، لأن Avalonia لا يدعم الإشارة لملف مفرد بهذي الصيغة
- [x] المحاولة 3: مجلد فرعي منفصل يحتوي ملف Regular وحده (`Assets/Fonts/Regular`)، بنفس صيغة المجلد اللي نجحت بالتحميل في المحاولة الأولى، لاختبار فرضية "تعدد الملفات" بشكل صحيح هذي المرة - فشلت بنفس خطأ المحاولة 2 بالضبط ("Could not create glyphTypeface")
- [x] اكتُشف السبب الحقيقي: `RealEstateInstallmentsManager.csproj` ما كان فيه `AvaloniaResource` لمجلد `Assets/Fonts` من الأصل، فكل مسار `avares://.../Assets/Fonts/...` (بكل المحاولات الثلاث، حتى الأولى) كان يشاور على مجلد فاضي - محرك Avalonia ما ضمّن ملفات الخط بالتطبيق أبدًا. تأكدت بفحص قائمة الموارد المُضمَّنة فعليًا في البناء (`obj/Debug/net10.0/Avalonia/resources`)
- [x] المحاولة 4 (الإصلاح الحقيقي): أضيف `<AvaloniaResource Include="Assets\Fonts\**" />` في ملف المشروع؛ تأكّدت بعد البناء أن كل ملفات .ttf صارت مُضمَّنة فعليًا. الشاشة كانت مثبّتة على ملف Regular وحده (نفس هدف المحاولة 3) عشان نغيّر متغيّر واحد بس
- [x] اختبر المطوّر المحاولة 4: التطبيق فتح بنجاح ("فتح زبط") - تأكّد أن سبب كل المشكلة كان التضمين الناقص، لا صيغة الخط ولا الملف نفسه
- [x] المحاولة 5: رجّعت المورد يشاور على مجلد `Assets/Fonts` كامل (الأربع أوزان)، بنفس الصيغة اللي استخدمتها المحاولة 1 بالضبط، والفرق الوحيد الحين أن الملفات فعلًا مُضمَّنة. حذفت مجلد `Assets/Fonts/Regular` الفرعي (كان نسخة مؤقتة لا داعي لها بعد الحين). البناء نجح والأربع ملفات ظاهرة في قائمة الموارد المُضمَّنة
- [x] اختبر المطوّر المحاولة 5 (الأربع أوزان): "كله ضبط" - الخط شغّال بكل أوزانه. المهمة منجزة

---

## 25. وضع داكن (Dark Mode) للهوية الجديدة (متوسط)
جهاز المطوّر على الوضع الداكن، وتصميم العميل المعتمد فاتح فقط. التطبيق الآن مثبَّت على `RequestedThemeVariant="Light"` دائمًا (`Main/App.axaml`) بغض النظر عن إعداد الجهاز، لأن اتباع النظام تلقائيًا كان يخلط ألوان الهوية الفاتحة مع عناصر Avalonia الجاهزة (أزرار، خانات، جداول) بألوانها الداكنة الافتراضية، فتظهر حالة غير متسقة ("حوسة ألوان").

- [x] تثبيت الوضع الفاتح دائمًا كحل مؤقت (Main/App.axaml)
- [ ] تصميم مجموعة ألوان داكنة توازي `Main/Theme.axaml` (نفس المتغيرات: خلفية، سطح، حبر، خط فاصل...)
- [ ] طريقة تبديل بين الوضعين (زر، أو اتباع النظام مرة أخرى بمجموعتي ألوان بدل واحدة)
- [ ] لا يبدأ العمل عليه إلا بعد أن يتأكد المطوّر أن الوضع الفاتح الحالي سليم بالكامل

---

## 26. إعادة تصميم شاشات الأقساط على هوية العميل (كبير)
بعد اعتماد الألوان (مهمة 16-25)، طلب المطوّر تجربة استخدام (UX) كاملة تطابق تصميم الـHTML الذي وافق عليه العميل، لا تلبيسًا بالألوان فقط: تخطيط عمودين، خانة مبلغ بارزة، لوحة معاينة حيّة، شريط تقدم أقساط شهري، وصناديق معلومات منظمة.

- [x] شاشة سند القبض (الأقساط) كتجربة أولى: تخطيط عمودين (نموذج يمين، تفاصيل العقد يسار)، لوحة معاينة حيّة (يغطي / المتبقي بعد السداد / حالة العقد بعده) تُحسب من بيانات موجودة بدون تغيير قاعدة البيانات، شريط تقدم أقساط تقديري (12 خانة أو حسب مدة العقد)، صناديق معلومات (منتج/عميل/مالك)، شارة حالة العقد. لم يتغيّر أي حساب أو حفظ فعلي، ولا كود المزامنة
- [x] `ContractServiceInstallment.FindByContractNum` صار يعيد كل بيانات العقد (بدلًا من مجموعة جزئية) عبر إعادة استخدام `GetById`
- [x] حماية إضافية: مدة العقد تُحدّ بحد أقصى معقول (600 شهرًا) قبل استخدامها في حساب التواريخ، لمنع تعطّل نظري من بيانات تالفة
- [ ] اختبار المطوّر الشامل لشاشة سند القبض قبل تعميم النمط
- [x] شاشة تسجيل الدخول: تخطيط عمودين (نموذج يمين، لوحة هوية غامقة يسار)، إظهار/إخفاء كلمة المرور، فحص اتصال حقيقي بدل نص ثابت (`ConnectivityService`)، بدون رابط "نسيت كلمة المرور" (ما فيه مسار فعلي)، وخانة "تذكرني" شكلية بدون وظيفة (موثّقة بالكود). راجعها code-reviewer بلا مشاكل حرجة
- [x] اكتُشفت ثغرة تصميم بشاشتي سند القبض وتسجيل الدخول: ما فيهم `FlowDirection="RightToLeft"` على مستوى الجذر (خلاف أغلب شاشات القوائم بالتطبيق)، فبعض النصوص والعناصر كانت تصطف يسار بدل يمين. أُصلحت بشاشة تسجيل الدخول (مع تبديل ترتيب أعمدة الـGrid لأن اتجاه RTL يعكس ترتيبها تلقائيًا)
- [ ] نفس الثغرة موجودة على الأغلب بشاشة سند القبض (16 من 47 عنصر نص فقط له محاذاة يمين صريحة) - بانتظار قرار المطوّر لإصلاحها
- [ ] تعميم نفس الأسلوب على باقي شاشات الأقساط (بعد الموافقة على التجربة الأولى)
- [ ] تعميمه على العقارات لاحقًا (له مدة عقد أيضًا، لكن أُجِّل حسب طلب المطوّر)
- [ ] أزرار "حفظ وواتساب" و"حفظ وطباعة" من التصميم المرجعي: مؤجَّلة (ميزات جديدة تحتاج تكامل واتساب)
