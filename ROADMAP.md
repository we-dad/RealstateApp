# ROADMAP

خارطة الطريق للمشروع. الترتيب في الجدول حسب الأهمية، وعمود "التنفيذ" هو الترتيب المنطقي للعمل.

- الأحجام تقديرية: **صغير** أقل من يوم، **متوسط** من 1 إلى 3 أيام، **كبير** أكثر من ذلك.
- كل بند يمس كود المزامنة (1 و2 و3 و4) يحتاج خطة وموافقة المطوّر قبل التنفيذ.
- بند 9 يحتاج إذن المطوّر لتغيير الحزم، ويأتي بعد بند 12 (اختبارات المزامنة) لأن تحديث الحزم قد يكسر البناء.
- بند 12: الاختبارات تعمل على SQLite مؤقت في الذاكرة مع سحابة وهمية، ولا تلمس القاعدة الحقيقية ولا Supabase.
- بند 13 ينفذه المطوّر يدويًا (Claude لا يتصل بـ Supabase). اختبار RLS العملي ضمن بند 8 لأنه يحتاج مشروع التطوير.
- أرقام عمود "#" معرّفات ثابتة ولا تتغير عند إعادة الترتيب.

## نظرة عامة

| الأهمية | # | البند | الحجم | التنفيذ | ملاحظة |
|---|---|---|---|---|---|
| 1 | 13 | مراجعة سياسات RLS في Supabase (نظريًا من اللوحة) | صغير | 2 | يدوي من المطوّر. الاختبار العملي ضمن بند 8 |
| 2 | 1 | إصلاح سباق الرفع/السحب | متوسط | 3 | يسبب تكرار صفوف. الحل النموذجي موجود في شاشة الوحدات |
| 3 | 2 | منع تكرار الصفوف في UpsertFromCloud | متوسط | 4 | يكمل بند 1 ويحمي من صفوف بلا `CloudId` |
| 4 | 5 | رسالة واضحة لدور tester | صغير | 1 | سريع، بدون مساس بمنطق المزامنة |
| 5 | 4 | توحيد منطق المزامنة في مكان واحد | كبير | 5 | يحوّل 12 نسخة إلى مكان واحد |
| 6 | 12 | اختبارات آلية لمنطق المزامنة | كبير | 6 | تُكتب بعد توحيد المنطق (بند 4) |
| 7 | 9 | تحديث الحزم الأربع ذات الثغرات الأمنية | متوسط | 7 | يحتاج إذن المطوّر. بعد بند 12 لأنه قد يكسر البناء |
| 8 | 3 | زر "استرجاع شامل" بمؤشر تقدم | كبير | 10 | يحتاج 1 و2 و4. يظهر لـ admin و editor و viewer فقط |
| 9 | 6 | نقل إعدادات Supabase إلى ملف خارجي | متوسط | 8 | شرط لبنود 7 و8 |
| 10 | 8 | مشروع Supabase منفصل للتطوير | صغير | 9 | يحتاج 6. الإنشاء يدوي من المطوّر. يشمل اختبار RLS العملي |
| 11 | 7 | وضع تجريبي ببيانات وهمية وبدون سحابة | كبير | 11 | يحتاج 6 |
| 12 | 10 | توقيع نسخة macOS | متوسط | 12 | يحتاج حساب Apple Developer |
| 13 | 11 | تطوير مستودع العرض (صور ووصف وفيديو) | كبير | 13 | ميزة جديدة، آخر أولوية |
| 0 | 14 | سلامة سندات القبض ورصيد العقد بين أكثر من جهاز | متوسط | 0 | عاجل: يمس المال. المرحلة 1 (اشتقاق الرصيد) منجزة ومختبرة، والمرحلة 2 (رقم سند لا يتعارض) لم تبدأ |
| 0 | 15 | إصلاح حسبة الدفعة المقدمة في صفحة تعديل العقد | صغير | 0 | منجز. يبقى فحص العقود التي انحفظت غلط قبل الإصلاح (بمراجعة المطوّر مع العميل) |

---

## 1. إصلاح سباق الرفع/السحب (متوسط)
الرفع والسحب يعملان معًا بدون انتظار في أغلب الشاشات، فقد تتكرر الصفوف. شاشة الوحدات وحدها تنتظر الرفع أولًا.

- [x] تحديد كل الشاشات التي تستدعي `PushAllDirtyAsync` و`Sync...FromCloudAsync` بدون انتظار
- [x] كتابة خطة والحصول على موافقة المطوّر
- [x] انتظار اكتمال الرفع قبل السحب في كل الشاشات (زر "تحديث" وفتح الشاشة): 11 شاشة قائمة (5 عقارات و6 أقساط) على فرع `fix-push-pull-race`، وبوابة الرفع صارت تنتظر بدل أن تُسقط الرفع الثاني، وأُضيفت بوابة لرفع الأقساط
- [x] مراجعة التعليق في `UnitsViewRealEstate` (السطران 38 و39) بعد الإصلاح: ما زال صحيحًا، وهو نفسه المنسوخ في بقية الشاشات
- [x] نافذتا العقود والمصاريف في الأقساط (`ContractWindowViewInstallment` و`ExpensesWindowViewInstallment`): كان لكل منهما نسخة رفع خاصة تتجاوز `InstallmentSyncService` وبوابته (المصاريف تستخدم خدمة العقارات، والعقود بلا حقول التوقيع). فحص المطوّر Supabase ولم يجد بيانات متضررة. استُبدلت النسختان باستدعاء `PushAllDirtyAsync` (commit `3250d98`)
- [x] اختبار المطوّر للنافذتين: تعديل عقد (التوقيع لا يُمسح)، وإضافة مصروف (يدخل `expenses_installment` وليس `expenses_real_estate`). أفاد المطوّر أن كل شيء سليم

## 2. منع تكرار الصفوف في UpsertFromCloud (متوسط)
المطابقة الآن بـ `CloudId` فقط، فصف محلي جديد لم يُرفع ثم نزل من السحابة يظهر مكررًا.

- [x] كتابة خطة والحصول على موافقة المطوّر
- [x] تحديد مفتاح مطابقة احتياطي لكل جدول عندما يكون `CloudId` فارغًا: الهوية والاسم للملاك والمستأجرين والعملاء، والرقم مع التاريخ والمبلغ والأب للعقود والإيصالات والمصاريف، والمالك مع الاسم والمدينة والحي والنوع والرقم للوحدات، ورقم الهيكل أو كل حقول المنتج للمنتجات
- [x] تطبيق المنطق في الخدمات الـ12 مع الحفاظ على حماية `IsDirty = 1`: التبنّي يغيّر `CloudId` و`SyncAction` فقط (من `insert` إلى `update`)، وخطأ `UNIQUE` في الجداول الستة يتخطى الصف ولا يوقف السحب. على فرع `fix-upsert-duplicates` (commits `37c5b65` و`f5dbfd8` و`96e74d8` وآخرها للوحدات والمنتجات)
- [x] اختبار المطوّر (الاستخدام العادي في كل الشاشات، وإضافة عقد وإيصال ومصروف، وجوالان بلونين مختلفين، ومبنى بوحداته): سليم، ثم دُمج الفرع في `claude-setup`
- [ ] (مهمة لاحقة) رسالة عربية للمستخدم عند تخطي صف بسبب تعارض رقم، لأن التخطي الآن يظهر في Console فقط، وإيصالات ومصاريف العقد المتخطى تُتخطى معه

## 3. زر "استرجاع شامل" (كبير)
يسحب كل الجداول بالترتيب الصحيح (الأب قبل الابن) مع مؤشر تقدم.

- [ ] كتابة خطة والحصول على موافقة المطوّر
- [ ] تحديد ترتيب السحب (المُلّاك والوحدات والمستأجرون قبل العقود، ثم الإيصالات والمصاريف، ومثله في التقسيط)
- [ ] إظهار الزر لأدوار admin و editor و viewer فقط (`AppSession.CanReadOnline`)
- [ ] إخفاء الزر في الوضع التجريبي (بند 7)
- [ ] مؤشر تقدم ورسالة نتيجة (عدد الصفوف وما تم تخطيه)
- [ ] عدم الكتابة فوق `IsDirty = 1` وعدم حذف أي صف محلي

## 4. توحيد منطق المزامنة (كبير)
منطق السحب مكرر في 12 شاشة.

- [ ] كتابة خطة والحصول على موافقة المطوّر
- [ ] استخراج المنطق المشترك إلى خدمة واحدة
- [ ] نقل الشاشات واحدة واحدة، مع بناء `dotnet build` بعد كل شاشة
- [ ] حذف الكود المكرر بعد اكتمال النقل

## 5. رسالة واضحة لمستخدم دور tester (صغير)
الدور الافتراضي `tester` لا يزامن شيئًا، والمستخدم لا يعرف السبب.

- [x] تحديد مكان عرض الرسالة (بعد الدخول أو داخل الشاشات)
- [x] كتابة رسالة عربية توضح أن الحساب بلا صلاحية مزامنة
- [x] عرضها عندما يكون الدور `tester`

## 6. نقل إعدادات Supabase إلى ملف خارجي (متوسط)
- [ ] تحديد الإعدادات الموجودة حاليًا في الكود
- [ ] اختيار مكان الملف (خارج المستودع) وصيغته
- [ ] القراءة من الملف مع رسالة واضحة إذا كان ناقصًا
- [ ] إضافة الملف إلى `.gitignore` وتوثيق الخطوات في README (بدون أي مفاتيح)

## 7. وضع تجريبي ببيانات وهمية وبدون سحابة (كبير)
شاشة اختيار عند الفتح: دخول عادي أو وضع تجريبي.

- [ ] تصميم شاشة الاختيار
- [ ] قاعدة SQLite منفصلة للوضع التجريبي (لا تلمس القاعدة الحقيقية)
- [ ] بيانات وهمية للوحدتين
- [ ] تعطيل كل استدعاءات السحابة في الوضع التجريبي
- [ ] إخفاء زر "استرجاع شامل" في الوضع التجريبي
- [ ] إشارة ظاهرة أن التطبيق في الوضع التجريبي

## 8. مشروع Supabase منفصل للتطوير (صغير)
ينفذه المطوّر يدويًا.

- [ ] إنشاء مشروع Supabase جديد للتطوير
- [ ] نسخ المخطط (schema) إليه من المشروع الحقيقي
- [ ] تفعيل RLS وتعطيل التسجيل العام فيه كما في الإنتاج
- [ ] ربطه عبر ملف الإعدادات الخارجي (بند 6)
- [ ] اختبار RLS عمليًا (من بند 13): تسجيل الدخول بحساب `tester` على مشروع التطوير ومحاولة القراءة والكتابة، ولا يُختبر ذلك على الإنتاج

## 9. تحديث الحزم الأربع ذات الثغرات الأمنية (متوسط)
الحزم: `Microsoft.IdentityModel.JsonWebTokens`، `System.IdentityModel.Tokens.Jwt`، `SQLitePCLRaw.lib.e_sqlite3`، `Tmds.DBus.Protocol`.

- [ ] الحصول على إذن المطوّر لتغيير الحزم
- [ ] تحديد الإصدارات الآمنة لكل حزمة
- [ ] تحديث حزمة واحدة في كل مرة مع `dotnet build`
- [ ] تشغيل التطبيق للتأكد من الدخول والمزامنة (ينفذه المطوّر)
- [ ] إزالة البند من قسم Known issues في `CLAUDE.md`

## 10. توقيع نسخة macOS (متوسط)
- [ ] التأكد من وجود حساب Apple Developer
- [ ] إنشاء شهادة التوقيع
- [ ] توقيع النسخة وإرسالها للتوثيق (notarization)
- [ ] توثيق الخطوات

## 11. تطوير مستودع العرض: صور ووصف وفيديو (كبير)
- [ ] تحديد الشاشات والجداول المعنية
- [ ] كتابة خطة تشمل التخزين (محلي أم سحابي) وحجم الملفات
- [ ] تصميم الشاشة
- [ ] التنفيذ

## 12. اختبارات آلية لمنطق المزامنة (كبير)
تعمل على SQLite مؤقت في الذاكرة مع سحابة وهمية، بدون Supabase وبدون القاعدة الحقيقية.

- [ ] إضافة مشروع اختبارات (يحتاج إذن المطوّر لأنه يضيف حزم NuGet)
- [ ] واجهة سحابة وهمية (fake) للاختبارات
- [ ] اختبار: السحب لا يكتب فوق `IsDirty = 1`
- [ ] اختبار: السحب لا يحذف صفوفًا محلية
- [ ] اختبار: تخطي الصفوف عند غياب الأب
- [ ] اختبار: منع التكرار (بند 2)
- [ ] اختبار: الرفع قبل السحب (بند 1)

## 13. مراجعة سياسات RLS في Supabase (صغير، يدوي)
الهدف: التأكد أن المستخدم المسجّل لا يقدر يقرأ بيانات غير مسموحة له. ينفذه المطوّر يدويًا، وClaude لا يتصل بـ Supabase. الاختبار العملي (بحساب tester) في بند 8 لأنه يحتاج مشروع التطوير.

الخطوات:
- [ ] فتح لوحة Supabase، ثم Authentication ثم Policies
- [ ] التأكد أن RLS مفعّل على كل الجداول (12 جدولًا للأعمال + جدول الأدوار)
- [ ] لكل جدول: قراءة السياسات والتأكد أن SELECT/INSERT/UPDATE/DELETE مربوطة بالدور (admin و editor و viewer) وليست `true` لأي مستخدم مسجّل
- [ ] التأكد أن دور `viewer` لا يقدر يكتب أو يحذف
- [ ] التأكد أن دور `tester` أو أي مستخدم بلا دور لا يقرأ أي جدول
- [ ] التأكد أن المستخدم لا يقدر يعدّل جدول الأدوار الخاص به (منع رفع صلاحيته لنفسه)
- [ ] تسجيل النتائج والملاحظات في `PROGRESS.md`

---

## 14. سلامة سندات القبض ورصيد العقد بين أكثر من جهاز (متوسط، عاجل)
اكتشفه فحص `sync-auditor`: رصيد العقد (`CurrentTotalAmount`) رقم مخزّن يتعدّل محليًا بدون أن يُعلَّم العقد للرفع، فيفوز آخر جهاز يكتب ولا يُدمج شيء. ورقم السند يُولَّد محليًا فيتعارض بين جهازين. وتعديل عقد عليه سندات يعيد رصيده للأصل.

**تحذير حتى يكتمل الإصلاح ويُدمج: لا تعدّل عقدًا عليه سندات، ولا تضف سندات لنفس العقد من جهازين بدون مزامنة بينهما.**

- [x] اشتقاق الرصيد من السندات: `الرصيد = المبلغ الأصلي − مجموع السندات غير المحذوفة` (لا ينزل عن صفر)، ويُعاد بعد إضافة سند أو تعديله أو حذفه، وبعد تعديل العقد، وبعد سحب السندات. وعند سحب العقد لا يُعاد الحساب إلا إذا كانت السندات موجودة محليًا (وإلا يبقى رصيد السحابة حتى تصل السندات). على فرع `fix-receipt-balance`
- [x] اختبار المطوّر للرصيد (إضافة سندين، وتعديل عقد عليه سندات، وحذف سند)، ثم دمج الفرع في `claude-setup`. أفاد المطوّر أن كل شيء سليم
- [x] رقم السند والمصروف بدون تعارض بين المستخدمين: رمز من 3 خانات مشتق من رقم حساب المستخدم (`Ir-1001-K7Q`)، مع دعم الأرقام القديمة، ولا ملف ولا إعداد. قرر المطوّر أن لكل شخص حسابه وجهازه، فاكتفينا برمز المستخدم بدل رمز الجهاز. على فرع `fix-document-number-collision`
- [x] رقم **العقد** صار بالرمز أيضًا (`Ic-1051-K7Q` و`Rc-1051-K7Q`)، والبحث في شاشات السندات يقبل الرقم القصير (`1051` أو `Ic-1051`) والكامل والقديم، وإذا تطابق أكثر من عقد يعرض الأرقام الكاملة ويطلب كتابة الكامل
- [ ] نافذة إيصالات العقارات (`ReceiptsWindowViewRealEstate`) لا تضيف البادئة `Rc-` عند البحث، فكتابة `1001` وحدها لا تنجح هناك (قبل التغيير وبعده)
- [ ] نفس الحساب على جهازين بدون اتصال قد يكرر الرقم (الرمز واحد للحساب). إن احتاج المطوّر ذلك يُضاف رقم جهاز
- [ ] السند المتخطى بسبب تعارض رقم لا يظهر للمستخدم (رسالة عربية)
- [ ] حذف سند على جهاز لا ينتقل للجهاز الآخر (السحب لا يحذف صفوفًا محلية): بند مستقل يغيّر قاعدة "السحب لا يحذف"، ويرتبط ببند 3
- [ ] فحص قيود Supabase على رقم السند (يدويًا من المطوّر)
- [ ] فحص سندات العقارات: هل فيها نفس التصميم؟
- [ ] لقطة الرصيد داخل السند (`CurrentTotalAmount` في `ReceiptsInstallment`) ما زالت تراكمية وقد تتضارب عبر الأجهزة، والـ PDF يطبعها في سطر 559
- [ ] عقود تضررت أرصدتها سابقًا: لا تُصلح تلقائيًا إلا عند أول عملية على سنداتها، وقرار إعادة حساب الكل يعود للمطوّر

---

## 15. حسبة الدفعة المقدمة في صفحة تعديل العقد (صغير)
كانت صفحة التعديل تعامل سعر المنتج الكامل كأنه سعر بعد الدفعة المقدمة، فتُحسب الدفعة مرتين، ويُحفظ الإجمالي بدون خصمها عند تغيير أي حقل.

- [x] إصلاح النافذة: السعر الكامل ثم خصم الدفعة، وعند تغيير المنتج، وعند إلغاء تحديد الدفعة أو مسح خانتها (`ContractWindowViewInstallment`)
- [x] تقرير "فحص الحسابات" للقراءة فقط في شاشة عقود الأقساط: يقارن الإجمالي المحفوظ بالمعادلة، ويعرض السعر الأصلي المقابل للمحفوظ (لو انخصمت الدفعة، ولو لم تنخصم) دون الاعتماد على سعر المنتج الحالي
- [ ] مراجعة المطوّر مع العميل للعقود المتضررة (حالة "بدون خصم الدفعة" أو لا يطابق سعر المنتج وقت العقد)، ثم قرار إصلاح كل عقد
- [ ] نفس خطأ إلغاء تحديد الدفعة في صفحة **إنشاء** العقد (`ContractViewInstallment`): لم يُصلح لأن المطوّر أفاد أن الإنشاء يعمل، وبانتظار قراره
- [ ] الوضع اليدوي: إلغاء الدفعة أو إعادتها يستبدل الإجمالي المكتوب يدويًا بالمحسوب. بانتظار قرار المطوّر
