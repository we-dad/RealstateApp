# PROGRESS

| التاريخ | المهمة | الحالة | ملاحظات |
|---|---|---|---|
| 2026-09-20 | بند 16: قوائم بحث ذكية | منجز | كل خانات البحث (العقد في السندات، والعميل والمستأجر بالهوية) صارت قوائم تعرض الاسم جنب الرقم. رقم العقد للقراءة فقط، وأرقام العقود والسندات والمصاريف بها رمز المستخدم. اختبرها المطوّر وأفاد أنها سليمة |
| 2026-09-20 | بند 17: تحديث البيانات تلقائيًا | قيد التنفيذ | المرحلة 1 مختبرة، ولوحتا التحكم تتحدثان محليًا. المرحلة 2 (سحب دوري كل دقيقتين) جُرّبت على شاشة الملاك ثم عُمّمت على الـ12 شاشة مع مؤشر مزامنة وفحص `CanWriteOnline`. بانتظار اختبار المطوّر للتعميم. وجدت المراجعة أن المزامنة الدورية كانت تعيد قوائم المنتج والوحدة لأول عنصر، فأُصلحت قبل الدمج |
| 2026-09-20 | بند 15: حسبة الدفعة المقدمة في صفحة تعديل العقد | منجز | إصلاح النافذة (الدفعة كانت تُحسب مرتين ويُحفظ الإجمالي بدون خصمها)، وتقرير "فحص الحسابات" للقراءة فقط. المتبقي: مراجعة المطوّر للعقود التي انحفظت غلط قبل الإصلاح (لا يصلحها البرنامج تلقائيًا)، وخطأ إلغاء الدفعة في صفحة الإنشاء |
| 2026-09-19 | بند 14: سلامة سندات القبض ورصيد العقد بين أكثر من جهاز | قيد التنفيذ | المرحلة 1 (اشتقاق الرصيد) اختبرها المطوّر وأفاد أنها سليمة. المرحلة 2: رمز مستخدم في رقم السند والمصروف (`Ir-1001-K7Q`) على فرع `fix-document-number-collision` (لم يُدمج بعد)، وأرقام العقود ما زالت تتعارض. فحص `sync-auditor` كشف أن رصيد العقد رقم مخزّن يتعدّل محليًا بدون رفع، فيفوز آخر جهاز، وأن تعديل عقد عليه سندات يعيد رصيده للأصل، وأن رقم السند يتعارض بين جهازين وسند الجهاز الآخر يُتخطى بصمت. المرحلة 1 (اشتقاق الرصيد من السندات) منفّذة على فرع `fix-receipt-balance` ومرّت على code-reviewer (وأُصلحت ثغرة وجدها: سحب العقد قبل السندات). المتبقي: اختبار المطوّر، ورقم سند لا يتعارض (المرحلة 2)، وبقية النقاط في ROADMAP |
| 2026-09-19 | بند 2: منع تكرار الصفوف في UpsertFromCloud | منجز | كل الخدمات الـ12 صارت "تتبنّى" الصف المحلي غير المرتبط (`CloudId = 0` و`IsDirty = 1` و`insert`) بدل إدخال نسخة مكررة، بمفتاح احتياطي لكل جدول (الرقم يُطابق مع حقول أخرى والأب، لأن أكثر من مستخدم قد يكوّنون نفس الرقم). التبنّي يغيّر `CloudId` و`SyncAction` فقط ولا يكتب فوق قيم محلية. خطأ `UNIQUE` في جداول العقود والإيصالات والمصاريف يتخطى الصف بدل إيقاف السحب. كل دفعة مرّت على code-reviewer. اختبر المطوّر الاستخدام العادي في كل الشاشات، وإضافة عقد وإيصال ومصروف، وجوالين بلونين مختلفين، ومبنى بوحداته: سليم. دُمج في `claude-setup` ولم يُدفع بعد. لم يُختبر التبنّي نفسه لأن إعادة إنتاجه على البيانات الحقيقية خطرة. مهمة لاحقة: رسالة عربية عند تخطي صف بسبب تعارض رقم |
| 2026-09-19 | بند 1: إصلاح سباق الرفع/السحب | منجز | تم على فرع `fix-push-pull-race` (commits `8408799` و`726c523` و`710b353` و`3dbbf48`): بوابة الرفع تنتظر بدل أن تُسقط، وبوابة جديدة للأقساط، و11 شاشة قائمة صارت تنتظر الرفع قبل السحب. اختبر المطوّر شاشة الملاك (عقارات) وشاشة العملاء (أقساط) بسجل تجريبي وضغط "تحديث" عدة مرات: سليم بلا تكرار. فحص المطوّر Supabase ولم يجد بيانات متضررة. ثم استُبدلت نسخ الرفع الخاصة في نافذتي العقود والمصاريف (أقساط) باستدعاء الخدمة (commit `3250d98`). اختبر المطوّر النافذتين (تعديل عقد، وإضافة مصروف) وأفاد أن كل شيء سليم. بقي دمج الـ Pull Request. دُمج في `master` عبر Pull Request #1 و#2 (حتى بند 2). البنود اللاحقة (14 وما بعدها) في Pull Request جديد |
| 2026-09-18 | بند 5: رسالة واضحة لدور tester | منجز | شريط تنبيه أعلى `MainMenuView` يظهر فقط عند `AppSession.IsTester`. تعديل ملفين (`MainMenuView.axaml` و`.axaml.cs`) بدون مساس بالمزامنة. البناء نجح، ومراجعة code-reviewer بلا مشاكل حرجة، وتحقق المطوّر بصريًا (يظهر لـ tester ومخفي لبقية الأدوار). commit `9dedd06` |

قيم عمود الحالة: `قيد التنفيذ` أو `منجز` أو `متوقف` (يُكتب سبب التوقف في الملاحظات).
